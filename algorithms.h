#ifndef ALGORITHMS_H
#define ALGORITHMS_H

/* Swap the contents of array[i] and array[j]. No bounds checking is done (or even can be done)
 *   so please only pass valid indices!
 */
void swap(int array[], size_t i, size_t j);

#endif
